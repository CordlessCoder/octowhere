[build]
target = "xtensa-esp32s3-none-elf"

[target.xtensa-esp32s3-none-elf]
# The partition table path is relative, so run cargo from the repository root.
runner = "espflash flash --monitor --log-format defmt --partition-table partitions.csv"
rustflags = ["-C", "link-arg=-Tlinkall.x", "-C", "link-arg=-Tdefmt.x"]

[unstable]
build-std = ["core", "alloc"]
build-std-features = ["optimize_for_size"]

[env]
# The level is fixed at compile time. "debug" adds the periodic sensor samples and the GNSS
# start-up trace.
DEFMT_LOG = "info"
# Provides a performance boost at the cost of using additional SRAM.
# Problem: without optimizations, uses way more ram.
# ESP_HAL_CONFIG_PLACE_ANON_IN_RAM = "true"
# These should be tuned carefully as they have a +- 15% perf impact on drawing and SPI flush.
ESP_HAL_CONFIG_DATA_CACHE_SIZE="64KB"
ESP_HAL_CONFIG_DATA_CACHE_LINE_SIZE="64B"
